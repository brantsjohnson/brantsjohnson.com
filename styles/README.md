# styles

Design tokens and global CSS. `tokens.css` holds colors, type, and motion variables. `globals.css` is the App Router entry that imports tokens and Tailwind layers. Components must use tokens, not hardcoded brand values.
