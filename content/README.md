# content

Static structured content that is not edited at runtime through the admin portal (legal pages, fixed bio fragments as MDX or JSON). Editable projects, media, and posts live in Supabase instead. Empty until we add static files.
