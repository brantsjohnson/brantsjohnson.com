// ============================================
// WHAT THIS FILE DOES (plain English):
// This holds the copy for the Contact page. Contact happens in one place
// only, so the email lives here and nowhere else on the site. The email
// itself is read from content/site.ts so it is never written twice.
// ============================================

// THIS SECTION DOES: the page intro and the one short human line above the email
export const contact = {
  eyebrow: "Contact",
  heading: "Brant S. Johnson",
  // A single warm line. No second call to action lives elsewhere on the site.
  note: "I read everything that arrives and reply to most of it.",
  ctaLabel: "Send a note",
};
