/**
 * Next.js configuration.
 *
 * Purpose: keep the site as a standard Next.js App Router project so it deploys
 * to Vercel with zero extra setup. `reactStrictMode` surfaces potential bugs in
 * development. Nothing here requires secrets or environment variables, so the
 * marketing pages build and render statically out of the box.
 */
/** @type {import('next').NextConfig} */
const nextConfig = {
  reactStrictMode: true,
};

export default nextConfig;
