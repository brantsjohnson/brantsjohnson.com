# components/ui

Small, reusable UI primitives (Button, Input, Card, and similar). These pieces stay dumb: they take props and render, with no page-specific layout or CMS fetching. Real components land when we start the design pass. Do not put section layouts or chatbot logic here.
