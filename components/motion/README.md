# components/motion

Reusable animation wrappers (FadeInOnScroll, StaggerChildren, GlassPanel, and similar). Every scroll reveal and transition should come from here so motion stays consistent. This folder does not own content or layout, only motion behavior. Empty until the UI pass.
