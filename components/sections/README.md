# components/sections

Page-level building blocks such as Hero, CurrentlyBuilding, AboutIntro, ProjectCard, ExperienceTimeline, and ContactBlock.

A section is opinionated about layout for one part of a page. If something is reusable everywhere, it belongs in `components/ui` instead. Copy still lives under `content/`. This folder does not fetch from a database.
