# components/sections

Page-level building blocks such as Hero, ProjectGrid, AboutBlock, and ContactForm. A section is opinionated about layout for one part of a page. If something is reusable everywhere, it belongs in `components/ui` instead. Empty until the UI pass.
