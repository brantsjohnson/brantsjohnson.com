# lib/integrations

Connectors to outside systems: CRM, GitHub, analytics helpers, and later social drafting support for Grok Bot. Each file should expose a small interface so feature flags can turn connectors on or off without rewriting pages.
