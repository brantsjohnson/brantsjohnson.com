# lib/ai

Server-side chatbot logic: retrieval, prompt assembly, and model calls. Used only by API routes. Never import these modules into client components. Private knowledge base access stays here and in Supabase RLS.
